import React from 'react'
import styles from "./components.module.css"

class ContactDetails extends React.Component {
  render () {
    return (
      <div className={`${styles.big}`}>This is the imprint section</div>
    )
  }
}

export default ContactDetails
